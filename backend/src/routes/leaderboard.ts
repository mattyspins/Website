import { Router } from 'express';
// Legacy leaderboard routes - currently disabled
// The new manual leaderboard system is available at /api/manual-leaderboards

const router = Router();

// TODO: Implement legacy leaderboard endpoints or remove if not needed
// These were for the old automatic leaderboard system

export default router;
